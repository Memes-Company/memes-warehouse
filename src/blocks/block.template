import { DataSet, PipelineBlock } from '../types';
export async function %blockName%(dataset: DataSet, config: PipelineConfig) {
  return dataset;
}

export class %blockName% extends PipelineBlock {
  public name: string = %blockName%.name;
  async process(dataset: DataSet, currentPullRequest: LocaleAwarePullRequest): Promise<DataSet> {
    return Promise.resolve(dataset);
  }
}
